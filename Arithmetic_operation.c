#include <stdio.h>

int main()
{
    int a, b;
    printf("Enter number a\n");
    scanf("%d" "%d", &a ,&b);

    // printf("Enter number b\n");
    // scanf("%d", &b);

    printf("The sum is %d\n",a+b);

    printf("The substraction is %d\n",a-b);

    printf("The substraction is %d\n",abs(a-b)); // for absolute value use abs()

    printf("The multipication is %d\n", a*b);
    
    printf("The division is %d\n", a/b);

    return 0;
}
